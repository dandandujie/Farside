diff --git a/packages/agent-core-v2/src/agent/toolResultTruncation/toolResultTruncationService.ts b/packages/agent-core-v2/src/agent/toolResultTruncation/toolResultTruncationService.ts
index 8d436bbb..5c57392e 100644
--- a/packages/agent-core-v2/src/agent/toolResultTruncation/toolResultTruncationService.ts
+++ b/packages/agent-core-v2/src/agent/toolResultTruncation/toolResultTruncationService.ts
@@ -3,14 +3,15 @@
  *
  * Persists complete oversized text tool results through `storage`, addressed
  * under the current `scopeContext` agent root, and renders a model-visible
- * preview with an absolute file path rooted at `bootstrap.homeDir`. Bound at
- * Agent scope.
+ * archive metadata with an absolute file path rooted at `bootstrap.homeDir`.
+ * Bound at Agent scope.
  */
 
 import { randomUUID } from 'node:crypto';
 
 import { InstantiationType } from '#/_base/di/extensions';
 import { LifecycleScope, registerScopedService } from '#/_base/di/scope';
+import { estimateTokens } from '#/_base/utils/tokens';
 import { IAgentScopeContext } from '#/agent/scopeContext/scopeContext';
 import type { ExecutableToolResult } from '#/tool/toolContract';
 import { IBootstrapService } from '#/app/bootstrap/bootstrap';
@@ -22,8 +23,7 @@ import {
   type ToolResultTruncationInput,
 } from './toolResultTruncation';
 
-const TOOL_RESULT_MAX_CHARS = 50_000;
-const TOOL_RESULT_PREVIEW_CHARS = 2_000;
+const TOOL_RESULT_MAX_ESTIMATED_TOKENS = 2_048;
 
 const encoder = new TextEncoder();
 
@@ -44,7 +44,9 @@ export class ToolResultTruncationService implements IAgentToolResultTruncationSe
     input: ToolResultTruncationInput<T>,
   ): Promise<T> {
     const text = persistableToolResultText(input.result.output);
-    if (text === undefined || text.length <= TOOL_RESULT_MAX_CHARS) return input.result;
+    if (text === undefined) return input.result;
+    const estimatedTokens = estimateTokens(text);
+    if (estimatedTokens <= TOOL_RESULT_MAX_ESTIMATED_TOKENS) return input.result;
     if (input.result.truncated === true) return input.result;
 
     const saved = await this.saveToolResult(input.toolName, input.toolCallId, text);
@@ -52,7 +54,13 @@ export class ToolResultTruncationService implements IAgentToolResultTruncationSe
 
     return {
       ...input.result,
-      output: renderPersistedToolResult(input.toolName, input.toolCallId, text, saved.outputPath),
+      output: renderPersistedToolResult(
+        input.toolName,
+        input.toolCallId,
+        text,
+        estimatedTokens,
+        saved.outputPath,
+      ),
       truncated: true,
     } as T;
   }
@@ -86,19 +94,18 @@ function renderPersistedToolResult(
   toolName: string,
   toolCallId: string,
   text: string,
+  estimatedTokens: number,
   outputPath: string,
 ): string {
   const lines = [
-    `Tool output exceeded ${String(TOOL_RESULT_MAX_CHARS)} characters; showing a preview only.`,
+    `Tool output exceeded ${String(TOOL_RESULT_MAX_ESTIMATED_TOKENS)} estimated tokens; the full output was archived.`,
     `tool_name: ${toolName}`,
     `tool_call_id: ${toolCallId}`,
+    `output_estimated_tokens: ${String(estimatedTokens)}`,
     `output_size_chars: ${String(text.length)}`,
     `output_size_bytes: ${String(Buffer.byteLength(text, 'utf8'))}`,
     `output_path: ${outputPath}`,
     'next_step: Use Read with output_path to page through the full output.',
-    '',
-    '[preview]',
-    text.slice(0, TOOL_RESULT_PREVIEW_CHARS),
   ];
   return lines.join('\n');
 }
diff --git a/packages/agent-core-v2/test/agent/toolResultTruncation/toolResultTruncation.test.ts b/packages/agent-core-v2/test/agent/toolResultTruncation/toolResultTruncation.test.ts
index 977dbe93..8019b032 100644
--- a/packages/agent-core-v2/test/agent/toolResultTruncation/toolResultTruncation.test.ts
+++ b/packages/agent-core-v2/test/agent/toolResultTruncation/toolResultTruncation.test.ts
@@ -42,7 +42,7 @@ describe('ToolResultTruncationService', () => {
     await rm(homeDir, { recursive: true, force: true });
   });
 
-  it('persists oversized string output and renders a bounded model preview', async () => {
+  it('persists oversized string output and renders archive metadata only', async () => {
     const fullOutput = `${'x'.repeat(50_001)}tail survives on disk`;
 
     const result = await truncation.truncateForModel<ExecutableToolResult>({
@@ -56,17 +56,20 @@ describe('ToolResultTruncationService', () => {
     const rendered = result.output;
     expect(typeof rendered).toBe('string');
     if (typeof rendered !== 'string') throw new Error('expected string output');
-    expect(rendered).toContain('Tool output exceeded 50000 characters');
+    expect(rendered).toContain('Tool output exceeded 2048 estimated tokens');
+    expect(rendered).toContain('output_estimated_tokens: 12506');
     expect(rendered).toContain('tool_name: Lookup Tool');
     expect(rendered).toContain('tool_call_id: call:lookup');
+    expect(rendered).not.toContain('[preview]');
+    expect(rendered).not.toContain('x'.repeat(100));
     expect(rendered).not.toContain('tail survives on disk');
 
     const outputPath = renderedOutputPath(rendered);
-    expect(outputPath).toContain(
+    expect(outputPath.replaceAll('\\', '/')).toContain(
       join(
         homeDir,
         'sessions/workspace/session/agents/main/tool-results/Lookup_Tool-call_lookup-',
-      ),
+      ).replaceAll('\\', '/'),
     );
     await expect(readFile(outputPath, 'utf8')).resolves.toBe(fullOutput);
   });
@@ -92,6 +95,43 @@ describe('ToolResultTruncationService', () => {
     );
   });
 
+  it('uses the shared ASCII and non-ASCII token estimate at the 2048-token boundary', async () => {
+    const asciiAtBudget = { output: 'a'.repeat(2_048 * 4) };
+    const asciiOverBudget = { output: 'a'.repeat(2_048 * 4 + 1) };
+    const cjkAtBudget = { output: '界'.repeat(2_048) };
+    const cjkOverBudget = { output: '界'.repeat(2_049) };
+
+    await expect(
+      truncation.truncateForModel({
+        toolName: 'Lookup',
+        toolCallId: 'call_ascii_at',
+        result: asciiAtBudget,
+      }),
+    ).resolves.toBe(asciiAtBudget);
+    await expect(
+      truncation.truncateForModel({
+        toolName: 'Lookup',
+        toolCallId: 'call_cjk_at',
+        result: cjkAtBudget,
+      }),
+    ).resolves.toBe(cjkAtBudget);
+
+    const asciiResult = await truncation.truncateForModel({
+      toolName: 'Lookup',
+      toolCallId: 'call_ascii_over',
+      result: asciiOverBudget,
+    });
+    const cjkResult = await truncation.truncateForModel({
+      toolName: 'Lookup',
+      toolCallId: 'call_cjk_over',
+      result: cjkOverBudget,
+    });
+    expect(asciiResult).not.toBe(asciiOverBudget);
+    expect(cjkResult).not.toBe(cjkOverBudget);
+    expect(asciiResult.output).toContain('output_estimated_tokens: 2049');
+    expect(cjkResult.output).toContain('output_estimated_tokens: 2049');
+  });
+
   it('keeps already-truncated and mixed-media results unchanged', async () => {
     const alreadyTruncated = {
       output: 'z'.repeat(50_001),
diff --git a/packages/agent-core/src/agent/turn/tool-result-budget.ts b/packages/agent-core/src/agent/turn/tool-result-budget.ts
index 2808544b..7922f3c1 100644
--- a/packages/agent-core/src/agent/turn/tool-result-budget.ts
+++ b/packages/agent-core/src/agent/turn/tool-result-budget.ts
@@ -5,9 +5,9 @@ import type { ContentPart } from '@moonshot-ai/kosong';
 import { join } from 'pathe';
 
 import type { ExecutableToolResult } from '../../loop';
+import { estimateTokens } from '../../utils/tokens';
 
-const TOOL_RESULT_MAX_CHARS = 50_000;
-const TOOL_RESULT_PREVIEW_CHARS = 2_000;
+const TOOL_RESULT_MAX_ESTIMATED_TOKENS = 2_048;
 
 interface BudgetToolResultOptions {
   readonly homedir?: string;
@@ -20,7 +20,9 @@ export async function budgetToolResultForModel(
   options: BudgetToolResultOptions,
 ): Promise<ExecutableToolResult> {
   const text = persistableToolResultText(options.result.output);
-  if (text === undefined || text.length <= TOOL_RESULT_MAX_CHARS) return options.result;
+  if (text === undefined) return options.result;
+  const estimatedTokens = estimateTokens(text);
+  if (estimatedTokens <= TOOL_RESULT_MAX_ESTIMATED_TOKENS) return options.result;
   if (options.result.truncated === true) return options.result;
   if (options.homedir === undefined) return options.result;
 
@@ -29,7 +31,13 @@ export async function budgetToolResultForModel(
     text,
   );
   if (outputPath === undefined) return options.result;
-  const output = renderPersistedToolResult(options.toolName, options.toolCallId, text, outputPath);
+  const output = renderPersistedToolResult(
+    options.toolName,
+    options.toolCallId,
+    text,
+    estimatedTokens,
+    outputPath,
+  );
   return options.result.isError === true
     ? { ...options.result, output, isError: true }
     : { ...options.result, output };
@@ -67,18 +75,19 @@ function renderPersistedToolResult(
   toolName: string,
   toolCallId: string,
   text: string,
+  estimatedTokens: number,
   outputPath: string,
 ): string {
   const lines = [
-    `Tool output exceeded ${String(TOOL_RESULT_MAX_CHARS)} characters; showing a preview only.`,
+    `Tool output exceeded ${String(TOOL_RESULT_MAX_ESTIMATED_TOKENS)} estimated tokens; the full output was archived.`,
     `tool_name: ${toolName}`,
     `tool_call_id: ${toolCallId}`,
+    `output_estimated_tokens: ${String(estimatedTokens)}`,
     `output_size_chars: ${String(text.length)}`,
     `output_size_bytes: ${String(Buffer.byteLength(text, 'utf8'))}`,
     `output_path: ${outputPath}`,
     'next_step: Use Read with output_path to page through the full output.',
   ];
-  lines.push('', '[preview]', text.slice(0, TOOL_RESULT_PREVIEW_CHARS));
   return lines.join('\n');
 }
 
diff --git a/packages/agent-core/test/agent/tool.test.ts b/packages/agent-core/test/agent/tool.test.ts
index 13ad93cb..1ae3e87f 100644
--- a/packages/agent-core/test/agent/tool.test.ts
+++ b/packages/agent-core/test/agent/tool.test.ts
@@ -447,7 +447,9 @@ describe('Agent tools', () => {
 
       const toolText = ctx.compactHistory().find((message) => message.role === 'tool')?.text ?? '';
       const outputPath = /^output_path: (.+)$/m.exec(toolText)?.[1];
-      expect(toolText).toContain('Tool output exceeded 50000 characters');
+      expect(toolText).toContain('Tool output exceeded 2048 estimated tokens');
+      expect(toolText).not.toContain('[preview]');
+      expect(toolText).not.toContain('x'.repeat(100));
       expect(toolText).not.toContain('tail survives');
       expect(outputPath).toBeTruthy();
       expect(readFileSync(outputPath!, 'utf8')).toBe(largeOutput);
